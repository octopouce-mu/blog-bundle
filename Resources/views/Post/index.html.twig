{% extends 'base.html.twig' %}

{% block title %}Blog{% endblock %}

{% block body %}
    <ul>
        {% for post in posts %}
            <li><a href="{{ path('octopouce_blog_post_show', {'slug':post.slug}) }}">{{ post.title }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}

